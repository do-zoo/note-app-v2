import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  containerHeader: {
    height: "100%",
  },
  groupHeader: {
    height: "100%",
  },
  buttonChangeTheme: {
    width: "36px",
    height: "36px",
  },
}));

export default useStyles;
